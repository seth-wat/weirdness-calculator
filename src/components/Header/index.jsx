import React from "react";
import { Row } from "../Row";

export const Header = props => (
  <Row>
    <h1 className="bp3-heading" style={{ padding: "1rem" }}>
      Weirdness Calculator
    </h1>
  </Row>
);
